[build]
  command = "mkdir -p publish && unzip -q site.zip -d publish"
  publish = "publish"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
