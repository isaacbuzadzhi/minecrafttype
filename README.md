# Prebuilt Voxel World

This repository is prepared for deployment through an existing Netlify project.

Netlify reads `netlify.toml`, extracts `site.zip` into the `publish` directory, and publishes that directory. The bundled game automatically opens the included CopeCraft world.

Do not extract or rename `site.zip` inside the GitHub repository.
